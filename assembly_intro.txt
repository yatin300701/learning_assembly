Each PC has micro processor that manages the computers arithmetical, logical, and control activites

Each processor has its own instructions.
These are written strings of 1's and 0's
Called ML instructions

To make it redable we have low level assembly language, created for specific 
family of processors.
 

 Every cpu does three  steps continuously
 1. Featch
 2. Decode 
 3. Execute


 CPU reads the next instructions from memory ( memory is collections of bytes - which is 0's and 1's)
 eg 

Memory

Address Data
1000    MOV
1001    AX
1002    BX


CPU figures out what does these instructions means
MOV AX, BX

CPU actually performs this instruction

Then repeat

